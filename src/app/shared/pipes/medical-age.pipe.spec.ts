import { MedicalAgePipe } from './medical-age.pipe';

describe('MedicalAgePipe', () => {
  it('create an instance', () => {
    const pipe = new MedicalAgePipe();
    expect(pipe).toBeTruthy();
  });
});
