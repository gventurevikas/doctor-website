import { MedicalPhonePipe } from './medical-phone.pipe';

describe('MedicalPhonePipe', () => {
  it('create an instance', () => {
    const pipe = new MedicalPhonePipe();
    expect(pipe).toBeTruthy();
  });
});
