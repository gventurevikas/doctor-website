import { MedicalFilterPipe } from './medical-filter.pipe';

describe('MedicalFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MedicalFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
