import { MedicalSortPipe } from './medical-sort.pipe';

describe('MedicalSortPipe', () => {
  it('create an instance', () => {
    const pipe = new MedicalSortPipe();
    expect(pipe).toBeTruthy();
  });
});
