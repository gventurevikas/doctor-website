import { MedicalPhoneDirective } from './medical-phone.directive';

describe('MedicalPhoneDirective', () => {
  it('should create an instance', () => {
    const directive = new MedicalPhoneDirective();
    expect(directive).toBeTruthy();
  });
});
