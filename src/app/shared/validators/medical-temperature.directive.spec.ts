import { MedicalTemperatureDirective } from './medical-temperature.directive';

describe('MedicalTemperatureDirective', () => {
  it('should create an instance', () => {
    const directive = new MedicalTemperatureDirective();
    expect(directive).toBeTruthy();
  });
});
