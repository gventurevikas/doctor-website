import { MedicalBpDirective } from './medical-bp.directive';

describe('MedicalBpDirective', () => {
  it('should create an instance', () => {
    const directive = new MedicalBpDirective();
    expect(directive).toBeTruthy();
  });
});
