import { MedicalAgeDirective } from './medical-age.directive';

describe('MedicalAgeDirective', () => {
  it('should create an instance', () => {
    const directive = new MedicalAgeDirective();
    expect(directive).toBeTruthy();
  });
});
