import { MedicalHeightDirective } from './medical-height.directive';

describe('MedicalHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new MedicalHeightDirective();
    expect(directive).toBeTruthy();
  });
});
