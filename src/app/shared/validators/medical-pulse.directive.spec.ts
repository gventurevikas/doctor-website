import { MedicalPulseDirective } from './medical-pulse.directive';

describe('MedicalPulseDirective', () => {
  it('should create an instance', () => {
    const directive = new MedicalPulseDirective();
    expect(directive).toBeTruthy();
  });
});
