import { MedicalDateDirective } from './medical-date.directive';

describe('MedicalDateDirective', () => {
  it('should create an instance', () => {
    const directive = new MedicalDateDirective();
    expect(directive).toBeTruthy();
  });
});
