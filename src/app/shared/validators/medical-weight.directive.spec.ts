import { MedicalWeightDirective } from './medical-weight.directive';

describe('MedicalWeightDirective', () => {
  it('should create an instance', () => {
    const directive = new MedicalWeightDirective();
    expect(directive).toBeTruthy();
  });
});
