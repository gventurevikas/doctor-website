import { MedicalEmailDirective } from './medical-email.directive';

describe('MedicalEmailDirective', () => {
  it('should create an instance', () => {
    const directive = new MedicalEmailDirective();
    expect(directive).toBeTruthy();
  });
});
