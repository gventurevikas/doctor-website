import { MedicalSsnDirective } from './medical-ssn.directive';

describe('MedicalSsnDirective', () => {
  it('should create an instance', () => {
    const directive = new MedicalSsnDirective();
    expect(directive).toBeTruthy();
  });
});
