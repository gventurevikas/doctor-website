import { MedicalDebounceDirective } from './medical-debounce.directive';

describe('MedicalDebounceDirective', () => {
  it('should create an instance', () => {
    const directive = new MedicalDebounceDirective();
    expect(directive).toBeTruthy();
  });
});
