import { MedicalScrollDirective } from './medical-scroll.directive';

describe('MedicalScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new MedicalScrollDirective();
    expect(directive).toBeTruthy();
  });
});
