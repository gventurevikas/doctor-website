import { MedicalResizeDirective } from './medical-resize.directive';

describe('MedicalResizeDirective', () => {
  it('should create an instance', () => {
    const directive = new MedicalResizeDirective();
    expect(directive).toBeTruthy();
  });
});
