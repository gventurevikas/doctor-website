import { MedicalAutofocusDirective } from './medical-autofocus.directive';

describe('MedicalAutofocusDirective', () => {
  it('should create an instance', () => {
    const directive = new MedicalAutofocusDirective();
    expect(directive).toBeTruthy();
  });
});
