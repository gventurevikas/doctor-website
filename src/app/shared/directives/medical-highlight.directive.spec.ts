import { MedicalHighlightDirective } from './medical-highlight.directive';

describe('MedicalHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new MedicalHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
