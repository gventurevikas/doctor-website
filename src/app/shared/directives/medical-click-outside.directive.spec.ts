import { MedicalClickOutsideDirective } from './medical-click-outside.directive';

describe('MedicalClickOutsideDirective', () => {
  it('should create an instance', () => {
    const directive = new MedicalClickOutsideDirective();
    expect(directive).toBeTruthy();
  });
});
