import { MedicalPermissionDirective } from './medical-permission.directive';

describe('MedicalPermissionDirective', () => {
  it('should create an instance', () => {
    const directive = new MedicalPermissionDirective();
    expect(directive).toBeTruthy();
  });
});
